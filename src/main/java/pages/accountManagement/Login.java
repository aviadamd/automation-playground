package pages.accountManagement;

import org.openqa.selenium.WebDriver;
import pages.InitWebPages;

public class Login extends InitWebPages {

    public Login(WebDriver driver) {
        super(driver);
    }
}
